{{- if .Values.chatserver.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "chatserver.name" . }}
  labels:
    {{- include "chatserver.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "chatserver.name" . }}
  minReplicas: {{ .Values.chatserver.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.chatserver.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.chatserver.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.chatserver.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.chatserver.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.chatserver.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}