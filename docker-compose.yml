services:
  gopie:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    volumes:
      - .:/app
      - ./data:/dataful
    networks:
      - gopie

networks:
  gopie:
